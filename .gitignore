# Working clones that live here for convenience but are not submodules.
# SciRecast tracks RecastEngine plus the CESM case's components under cesm/.
# Everything below is checked out beside them so the migration work has its
# sources at hand, and must never be committed.
/recast-cesm/
/CESM-language-translator/
/CESM-Agent-Produced-Scripts/

# macOS
.DS_Store
# PyCAM5 is pinned at cesm/PyCAM5; a copy at the top level is a scratch clone,
# and committing it would ship an embedded repository nobody can fetch.
/PyCAM5/
