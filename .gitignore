# Working clones that live here for convenience. This repository is the site and
# tracks none of them -- each component is its own repository, linked from the
# pages, so anything checked out beside them must never be committed.
/recast-cesm/
/RecastEngine/
/cesm/

# macOS
.DS_Store
# A top-level PyCAM5 is a scratch clone; committing it would ship an embedded
# repository nobody can fetch.
/PyCAM5/
